require 'spec_helper'

describe AcceptItOnProd do
  it 'has a version number' do
    expect(AcceptItOnProd::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
